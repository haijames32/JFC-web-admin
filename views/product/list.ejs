<%- include('../includes/header.ejs') %>

   <style>
      .item-cate:hover {
         color: #e31836;
      }
   </style>



   <div class="container col-9 d-flex flex-row align-items-center"
      style="height: 80px; border-bottom-left-radius: 20px; border-bottom-right-radius: 20px; justify-content: center; background-color: #f8dbe0;">
      <% listCate.forEach(item=>{ %>
         <a href="/products/cate/<%= item._id %>"
            class="d-flex flex-column align-items-center rounded text-black item-cate"
            style="width: 115px; height: 60px; margin-left: 8px; margin-right: 8px; cursor: pointer;">
            <img src="<%= item.image %>" class="rounded" style="width: 40px; height: 40px;">
            <% if(cateId==item._id){ %>
               <span style="font-size: 13px; font-weight: bold; color: #e31836;">
                  <%= item.name %>
               </span>
               <% }else{ %>
                  <span style="font-size: 13px; font-weight: bold;">
                     <%= item.name %>
                  </span>
                  <% } %>
         </a>
         <% }) %>
   </div>

   <hr>

   <div class="container col-8">
      <div class="d-flex mb-3">
         <div class="container d-flex align-items-center" style="justify-content: space-between;">
            <div class="d-flex col me-3" style="justify-content: space-between;">
               <a href="/products" class="btn text-white btn-primary" data-mdb-ripple-init>
                  <i class="fas fa-refresh"></i>
               </a>
               <!-- Checkbox All -->
               <!-- <div class="form-check mt-2 ms-2" style="width: 109px;">
                  <input class="form-check-input checkbox-all" type="checkbox" value="" id="checkbox-all" />
                  <label class="form-check-label" for="checkbox-all">Chọn tất cả</label>
               </div> -->
               <!-- Choose Actions -->
               <!-- <div class="col-3">
                  <select class="form-select ms-2" aria-label="Default select example" required>
                     <option value="" selected>Chọn hành động</option>
                     <option value="delete">Xóa</option>
                  </select>
               </div> -->
               <!-- <button type="submit" class="btn btn-primary ms-3" id="btn-check-all-submit" disabled
                  data-mdb-ripple-init>OK</button> -->
               <!-- <form method="get" action="" class="d-flex">
                  <div>
                     <select class="form-select ms-2" aria-label="Default select example" name="filterCate" required>
                        <option value="" selected>Thể loại</option>
                        <% listCate.forEach((item, index)=>{ %>
                           <% if(index==indexCate){ %>
                              <option value="<%= item._id %>" selected="selected">
                                 <%= item.name %>
                              </option>
                              <% }else{ %>
                                 <option value="<%= item._id %>">
                                    <%= item.name %>
                                 </option>
                                 <% } %>

                                    <% }) %>
                     </select>
                  </div>
                  <button type="submit" class="btn btn-primary ms-3" data-mdb-ripple-init>OK</button>
               </form> -->
            </div>

            <div class="d-flex">
               <div class="me-3">
                  <div class="input-group">
                     <form class="d-flex" action="" method="get">
                        <div class="form-outline" data-mdb-input-init>
                           <input type="search" id="form1" class="form-control" style="width: 170px;"
                              value="<%= search %>" name="searchName" />
                           <label class="form-label" for="form1">Search</label>
                        </div>
                        <button type="submit" class="btn btn-primary" style="z-index: 0;" data-mdb-ripple-init>
                           <i class="fas fa-search"></i>
                        </button>
                     </form>
                  </div>
               </div>
               <button type="button" class="btn text-white btn-primary me-3" data-mdb-ripple-init data-mdb-modal-init
                  data-mdb-target="#modalAddProduct">
                  <i class="fas fa-plus"></i>
               </button>
               <a href="/trash" class="btn text-white btn-primary " data-mdb-ripple-init>
                  <i class="fas fa-trash"></i>
                  <span class="badge rounded-pill badge-notification bg-danger" style="top: -8px; right: -4px;">
                     <%= listProdDeleted %>
                  </span>
               </a>
            </div>
         </div>
      </div>
      <!-- Modal Add Product -->
      <%- include('./includes/modalAdd.ejs') %>

         <table class="table table-hover table-bordered table-sm align-middle mb-0 bg-white">
            <thead class="bg-light">
               <tr class="table-secondary">
                  <th scope="col" style="font-size: 15px;">#</th>
                  <th scope="col" style="font-size: 15px;">Image</th>
                  <th scope="col" style="font-size: 15px;">Name</th>
                  <th scope="col" style="font-size: 15px;">Price</th>
                  <th scope="col" style="font-size: 15px;">Category</th>
                  <th scope="col" style="font-size: 15px;">Actions</th>
               </tr>
            </thead>
            <tbody>
               <% listProd.forEach((item,index)=>{ %>
                  <tr>
                     <td>
                        <%= index + 1 %>
                     </td>
                     <td>
                        <div class="align-items-center">
                           <img src="<%= item.image %>" class="rounded" alt="" style="width: 45px; height: 45px" />
                        </div>
                     </td>
                     <td>
                        <%= item.name %>
                     </td>
                     <td>
                        <%= item.price.toLocaleString() %>₫
                     </td>
                     <td>
                        <%= item.category.name %>
                     </td>
                     <td>
                        <a class="btn text-white btn-sm btn-floating bg-primary" href="/products/<%= item._id %>/edit"
                           data-mdb-ripple-init>
                           <i class="fas fa-pen"></i>
                        </a>
                        <a class="btn text-white btn-sm btn-floating" data-mdb-ripple-init data-mdb-modal-init
                           data-mdb-id="<%= item._id %>" data-mdb-target="#modalDelete"
                           style="background-color: #c61118;">
                           <i class="fas fa-trash"></i>
                        </a>
                     </td>
                  </tr>
                  <%}) %>
            </tbody>
         </table>
         <% if(listProd.length==0){ %>
            <div class="text-center mt-4">
               <div>
                  <img src="https://img.icons8.com/color/96/general-warning-sign.png" class="img rounded"
                     style="width: 80px; height: 80px;" alt="">
               </div>
               <h5 class="fw-semibold text-muted">Không có sản phẩm nào..</h5>
            </div>
            <% } %>

               <!-- <div class="container d-flex col" style="justify-content: center;">
               <div class="btn-toolbar mt-4" role="toolbar" aria-label="Toolbar with button groups">
                  <form action="" method="get">
                     <div class="btn-group me-2" role="group" aria-label="First group">
                        <% if(skipProd==0){ %>
                           <button type="submit" class="btn" style="background-color: #b0ceff;" name="skipProduct"
                              value="<%= skipProd - 5 %>" disabled data-mdb-ripple-init>
                              < </button>
                                 <% }else{ %>
                                    <button type="submit" class="btn" style="background-color: #b0ceff;"
                                       name="skipProduct" value="<%= skipProd - 5 %>" data-mdb-ripple-init>
                                       < </button>
                                          <% } %>

                     </div>
                     <div class="btn-group me-2" role="group" aria-label="Second group">
                        <button type="submit" class="btn btn-primary" value="0" name="skipProduct"
                           data-mdb-ripple-init>1</button>
                        <button type="submit" class="btn" style="background-color: #b0ceff;" value="5"
                           name="skipProduct" data-mdb-ripple-init>2</button>
                        <button type="submit" class="btn" style="background-color: #b0ceff;" value="10"
                           name="skipProduct" data-mdb-ripple-init>3</button>
                        <button type="submit" class="btn" style="background-color: #b0ceff;" value="15"
                           name="skipProduct" data-mdb-ripple-init>4</button>
                        <button type="submit" class="btn" style="background-color: #b0ceff;" value="20"
                           name="skipProduct" data-mdb-ripple-init>5</button>
                        <button type="submit" class="btn" style="background-color: #b0ceff;" value="25"
                           name="skipProduct" data-mdb-ripple-init>6</button>
                        <button type="submit" class="btn" style="background-color: #b0ceff;" value="30"
                           name="skipProduct" data-mdb-ripple-init>7</button>
                     </div>
                     <div class="btn-group" role="group" aria-label="Third group">
                        <% if(skipProd==10){ %>
                           <button type="submit" class="btn" style="background-color: #b0ceff;"
                              value="<%= skipProd + 5 %>" name="skipProduct" disabled data-mdb-ripple-init>></button>
                           <% }else{ %>
                              <button type="submit" class="btn" style="background-color: #b0ceff;"
                                 value="<%= skipProd + 5 %>" name="skipProduct" data-mdb-ripple-init>></button>
                              <% } %>

                     </div>
                  </form>
               </div>
            </div> -->
   </div>

   <!-- Modal Delete to Trash Product -->
   <%- include('./includes/modalDelete.ejs') %>

      <!-- Form Delete Product to trash -->
      <form method="post" name="deleteForm"></form>

      <script>
         // Code Delete Product to Trash
         document.addEventListener('DOMContentLoaded', () => {
            const modalDelete = document.getElementById('modalDelete');
            let productId
            let deleteForm = document.forms['deleteForm']

            // Event show modal DELETE
            modalDelete.addEventListener('show.mdb.modal', (e) => {
               const button = e.relatedTarget;
               productId = button.getAttribute('data-mdb-id');
            })

            const btnDelete = document.getElementById('btnDeleteProductModal')
            // Click DELETE in Modal delete
            btnDelete.onclick = function () {
               deleteForm.action = `/products/${productId}?_method=DELETE`
               deleteForm.submit()
            }

            //Code feature checkbox options for item product
            // let formActionCheckbox = $('form[name="form-action-checkbox"]')
            // let checkboxAll = $('#checkbox-all')
            // let prodItemCheckbox = $('input[name="productIds"]')
            // let btnCheckAllSubmit = $('#btn-check-all-submit')

            // Input Checkbox All
            // checkboxAll.change(() => {
            //    let isChecked = $(this).prop('checked')
            //    prodItemCheckbox.prop('checked', isChecked)
            //    renderCheckAllSubmitBtn()
            // })

            // Input Checkbox Item Product
            // prodItemCheckbox.change(() => {
            //    let isCheckedAll = prodItemCheckbox.length == $('input[name="productIds"]:checked').length
            //    checkboxAll.prop('checked', isCheckedAll)
            //    renderCheckAllSubmitBtn()
            // })

            //EventListener form checkbox
            // formActionCheckbox.on('submit', function (e) {
            //    let isSubmitable = !btnCheckAllSubmit.hasClass('disabled')
            //    if (!isSubmitable) {
            //       e.preventDefault()
            //    }
            // })

            // Re-render check all submit button
            // const renderCheckAllSubmitBtn = () => {
            //    let checkedCount = $('input[name="productIds"]:checked').length
            //    if (checkedCount > 0) {
            //       btnCheckAllSubmit.attr('disabled', false)
            //    } else {
            //       btnCheckAllSubmit.attr('disabled', true)
            //    }
            // }
         })
      </script>


      <%- include('../includes/footer.ejs') %>